# Qora Demo Client

Client-scoped product configuration for the Qora demo tenant.

Agents for this client live under `agents/{agent-slug}/`. Runtime agent skills, when added, must stay inside the owning agent folder.
