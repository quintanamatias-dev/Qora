# Delta for call-sip-observability

## ADDED Requirements

(All requirements are new — no prior spec exists for this capability.)

See full source spec at: `openspec/specs/call-sip-observability/spec.md`

This delta references the full spec. No modifications to existing capabilities are
included here. All requirements are net-new additions introduced by the
`call-observability-reconciliation` change.
