# API base URL — leave empty for same-origin (proxied via Vite)
VITE_API_BASE_URL=

# Admin API key (Phase B5). Must match QORA_API_KEY in backend/.env.
# Phase C: replaced by JWT from login flow.
VITE_API_KEY=
